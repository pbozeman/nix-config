#!/bin/sh -e

FLAKE=$(hostname -s)
SYSTEM="packages.x86_64-linux.proxmox"

# Navigate to the directory of this script
cd $(dirname $(readlink -f $0))
cd ..

echo "Starting..."
nix --experimental-features 'nix-command flakes' build .#$SYSTEM $@

echo "Done"
