#! /usr/bin/env bash

set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "Usage: gwtrm <name>" >&2
  exit 1
fi

name="$1"

# Pure worktree removal (remove worktree, delete local + dev branch).
git-worktree-rm "$@"

# Tear down the dev-env window.
tmux kill-window -t "$name" 2>/dev/null || true
